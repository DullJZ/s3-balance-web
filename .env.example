# S3 Balance Web 环境变量配置示例
# 复制此文件为 .env.local 并根据实际情况修改

# ========== 后端 API 配置 ==========

# 后端 API 基础地址
# 注意：此配置会在构建时嵌入到应用中
# 也可以在运行时通过界面配置（系统配置 > 前端配置）动态修改

# 本地开发（默认）
VITE_API_BASE_URL=http://localhost:8080

# 其他常见配置示例：
# 局域网开发
# VITE_API_BASE_URL=http://192.168.1.100:8080

# 远程开发服务器
# VITE_API_BASE_URL=http://dev-server.example.com:8080

# 生产环境（HTTPS）
# VITE_API_BASE_URL=https://api.example.com

# Nginx反向代理（前后端同域）
# VITE_API_BASE_URL=/

# Docker容器访问宿主机
# VITE_API_BASE_URL=http://host.docker.internal:8080

# ========== 其他配置 ==========

# 应用部署路径（重要！）
# 如果应用部署在子路径下（如 example.com/web/），必须配置此项
# 注意：路径必须以 / 开头和结尾，例如 /web/ 或 /admin/
# 如果部署在根路径（example.com/），请设置为 /
VITE_BASE_PATH=/web/

# 应用标题（可选）
# VITE_APP_TITLE=S3 Balance 管理面板

# 应用版本（可选）
# VITE_APP_VERSION=1.0.0
